<!-- ============================================================
   PostGrid.astro — Blog post card grid container
   Desktop: 2 columns, Mobile: 1 column
   ============================================================ -->

---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={["post-grid", className].filter(Boolean).join(" ")}>
  <slot />
</div>

<style>
  .post-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
  }

  @media (min-width: 640px) {
    .post-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .post-grid {
      gap: var(--space-8);
    }
  }
</style>
